node_modules/
.vercel
.DS_Store
Thumbs.db
*.bak
__pycache__/
.env
.env.*

# Salida vieja del build anterior, ya no se genera.
carpa-artifact.html

# Carpa-de-Saberes.html SÍ se versiona: es el entregable que se reparte por USB.
# sitio/js/datos-*.js también se versionan aunque build.py los copie desde la
# raíz: el despliegue es estático puro, sin paso de build, así que si no están
# en el repo el sitio se publica sin contenido.
